export default {
  adminLogin(context, obj) {
    context.commit('login', obj);
  },
  // push (context) {
  //   context.commit('push')
  // },
  // remove(context, params) {
  //   context.commit('remove', params)
  // },
  // pop(context) {
  //   context.commit('pop')
  // }
};
